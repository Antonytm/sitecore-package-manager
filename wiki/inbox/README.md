# Inbox

Drop raw, unprocessed sources here: articles, docs, pasted threads, decision notes, research — any format Claude can read.

Run `/wiki-compile` to compile them into `wiki/articles/`. Processed files are moved to `wiki/sources/`, so this folder should normally be empty (except this README, which is never compiled).
